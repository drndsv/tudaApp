import { rem } from "@mantine/core";
import { CSSProperties } from "react";

export const leftAlignedLabel: CSSProperties = {
  textAlign: "left",
  display: "block",
  width: "100%",
  paddingLeft: rem(12),
  marginBottom: rem(4),
};
